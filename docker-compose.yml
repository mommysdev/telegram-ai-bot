services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    # Uncomment if using Ollama on the same machine:
    # extra_hosts:
    #   - "host.docker.internal:host-gateway"
